import React from 'react';
import LearnMore from '../../components/LandingPage/LearnMore';

const FAQ = () => {
    return (
        <div className="min-h-screen bg-base-100">
            {/* Reusing existing LearnMore component as requested */}
            <LearnMore />
        </div>
    );
};

export default FAQ;
